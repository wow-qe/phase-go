coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 0%

comment:
  layout: "reach,diff,flags,files"
  require_changes: true
